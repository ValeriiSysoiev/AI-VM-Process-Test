# Example environment variables
# ID of the Azure subscription used for deployments
AZURE_SUBSCRIPTION_ID=
# Name of the Azure resource group
RESOURCE_GROUP=
# Azure region for the resource group
LOCATION=
# Name of the Key Vault that stores secrets for Nexpose
KEY_VAULT_NAME=
# Endpoint for the ingestion API or other services
API_ENDPOINT=
# Base URL for the orchestrator API (default http://localhost:8000)
BACKEND_API_URL=
# Toggle built-in EasyAuth (default True)
AUTH_ENABLED=
# Optional Power BI dashboard link used in the UI compliance page
POWERBI_EMBED_URL=
# Service principal credentials for azd deployments
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=
